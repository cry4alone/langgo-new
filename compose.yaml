services:
  langgonew:
    image: langgonew
    build:
      context: .
      dockerfile: LanggoNew/Dockerfile
    ports:
      - "8080:8080"
    env_file:
      - ./LanggoNew/.env
  postgres:
    image: postgres:15-alpine
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: langgo_admin
      POSTGRES_DB: langgo
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
  redis:
    image: redis:7-alpine
    restart: always
    volumes:
      - redis_data:/data
    ports:
      - "6379:6379"
  redisinsight:
    image: redis/redisinsight:3.2.0-arm64
    restart: always
    ports:
      - "5540:5540"
    volumes:
      - redisinsight_data:/db
  papercut:
    image: changemakerstudiosus/papercut-smtp
    ports:
      - "25:2525"
      - "80:8080"
#  miniio:
#    image: minio/minio:latest
#    command: server /data
#    environment:
#      MINIO_ROOT_USER: minioadmin
#      MINIO_ROOT_PASSWORD: minioadmin
#    ports:
#      - "9000:9000"
#      - "9001:9001"
#    volumes:
#      - minio_data:/data

volumes:
  postgres_data:
  redis_data:
  redisinsight_data:
  minio_data:
